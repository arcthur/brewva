structured_payload:
  incident_id: null
  severity: P1_or_P2
  affected_endpoint: /api/checkout
  symptom: HTTP 500 errors on checkout endpoint
  timeline:
    detected: "15:00"
    mitigated: "15:15"
    mitigation_action: rollback
  suspected_causes:
    - config change
    - new auth middleware
  supplemental_signals:
    - source: Sarah
      observation: memory spikes
  root_cause: null
  status: needs_triage
extraction_report: "Extracted 8 fields from incident thread. severity is ambiguous (P1 or P2) — kept as explicit uncertainty rather than guessing. incident_id and root_cause are null because source provides no ID and cause is unconfirmed. supplemental_signals preserved Sarah's memory observation as unverified supplemental data."
