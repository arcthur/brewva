change_set: "Added SkillTimingEvent interface to runtime contracts. Updated skill-complete handler in tools to emit timing event with duration_ms. Coordinated rollout: runtime type first, then tools emission."
files_changed:
  - packages/brewva-runtime/src/contracts/events.ts
  - packages/brewva-tools/src/skill-complete.ts
verification_evidence: "bun run check passes. bun test passes (43/43). New type exported correctly from @brewva/brewva-runtime."
