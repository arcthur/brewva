// Seam marker for the gateway-domain-seam-files invariant. The model-routing
// domain exposes its surface via api.ts / wiring.ts; it has no distinct consumer
// port, so this marker intentionally stays empty.
export interface ModelRoutingPort {}
