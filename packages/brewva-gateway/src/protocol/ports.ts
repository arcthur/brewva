// Seam marker for the gateway-domain-seam-files invariant. The protocol domain's
// contract is the wire-frame schemas (schema.ts); it exposes no distinct consumer
// port, so this marker intentionally stays empty.
export interface GatewayProtocolPort {}
