# Reference: Commands

Brewva's command surface is split across the root CLI, the interactive shell,
the gateway daemon, credentials/inspect/insights helpers, and channel hosts.
This entry page owns the generated subcommand/flag inventory and points to the
semantic pages for each operator surface.

Implementation anchors:

- `packages/brewva-cli/src/index.ts`
- `packages/brewva-gateway/src/cli.ts`

## Reading Path

- Interactive shell, overlays, completion, and keyboard contract:
  `docs/reference/commands/interactive.md`
- Gateway daemon lifecycle and operational flags:
  `docs/reference/commands/gateway.md`
- Credentials, inspect, insights, and onboard helpers:
  `docs/reference/commands/credentials-inspect-insights.md`
- Channel mode, Telegram inputs, and runtime-plugin slash commands:
  `docs/reference/commands/channel.md`

## Generated CLI Surface

<!-- generated:cli-flags start -->

> Generated by `bun run docs:inventory`. Do not edit this block by hand.

Root subcommand count: 5. Gateway subcommand count: 10. CLI flag count: 59.

### Root Subcommands

- `credentials`
- `gateway`
- `insights`
- `inspect`
- `onboard`

### Gateway Subcommands

- `heartbeat-reload`
- `install`
- `logs`
- `rotate-token`
- `scheduler-pause`
- `scheduler-resume`
- `start`
- `status`
- `stop`
- `uninstall`

### Long Flags

- `--agent`
- `--backend`
- `--channel`
- `--config`
- `--cwd`
- `--daemon`
- `--deep`
- `--detach`
- `--dry-run`
- `--force`
- `--foreground`
- `--from-env`
- `--health-http-path`
- `--health-http-port`
- `--heartbeat`
- `--help`
- `--host`
- `--install-daemon`
- `--interactive`
- `--json`
- `--label`
- `--launchd`
- `--log-file`
- `--managed-tools`
- `--max-open-queue`
- `--max-payload-bytes`
- `--max-workers`
- `--mode`
- `--model`
- `--no-start`
- `--pid-file`
- `--plist-file`
- `--port`
- `--print`
- `--reason`
- `--redo`
- `--ref`
- `--replay`
- `--service-name`
- `--session`
- `--session-idle-ms`
- `--state-dir`
- `--systemd`
- `--tail`
- `--task`
- `--task-file`
- `--telegram-callback-secret`
- `--telegram-poll-limit`
- `--telegram-poll-retry-ms`
- `--telegram-poll-timeout`
- `--telegram-token`
- `--tick-interval-ms`
- `--timeout-ms`
- `--token-file`
- `--undo`
- `--unit-file`
- `--verbose`
- `--version`
- `--wait-ms`
<!-- generated:cli-flags end -->

## Mode Selection

Primary modes:

- interactive mode by default
- one-shot text mode with `--print`
- one-shot JSON mode with `--mode json` or `--json`
- undo, redo, and replay modes
- scheduler daemon mode
- channel gateway mode

Mode resolution is fail-fast. Incompatible input surfaces return a parse error
instead of reviving retired prompt-loop behavior.

## Input Contract

- `--task` and `--task-file` are mutually exclusive.
- Prompt text overrides `TaskSpec.goal` when both are provided.
- Non-TTY stdin/stdout defaults to text print mode unless another mode is
  explicit.
- `--replay`, `--undo`, and `--redo` are mutually exclusive and cannot combine
  with task inputs.
- `--help` and `--version` return exit code `0`; parse and validation failures
  return exit code `1`.

## Authority Boundary

CLI flags and slash commands are operator veneers. They select host behavior or
runtime-plugin entrypoints; they do not create new kernel authority. Durable
receipt and event semantics live under `docs/reference/runtime.md` and
`docs/reference/events/README.md`.
