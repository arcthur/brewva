# Reference: Configuration

Configuration contract sources:

- Defaults: `packages/brewva-runtime/src/config/defaults.ts`
- Loader: `packages/brewva-runtime/src/config/loader.ts`
- Normalizer: `packages/brewva-runtime/src/config/normalize.ts`
- Type contract: `packages/brewva-runtime/src/config/types.ts`
- Schema: `packages/brewva-runtime/schema/brewva.schema.json`

Config files are patch overlays. Omitted fields inherit defaults or
lower-precedence layers. Files remain named `.json`, but the loader accepts
JSONC syntax for human-authored config.

## Generated Config Key Index

<!-- generated:config-keys start -->

> Generated by `bun run docs:inventory`. Do not edit this block by hand.

Top-level config key count: 11.

- `channels`
- `infrastructure`
- `ledger`
- `parallel`
- `projection`
- `schedule`
- `security`
- `skills`
- `tape`
- `ui`
- `verification`
<!-- generated:config-keys end -->

## Loading And Overrides

Construction-time runtime options are intentionally separate from persisted
config:

- `new BrewvaRuntime({ routingScopes })` is a hard override for skill routing
  scopes.
- `new BrewvaRuntime({ routingDefaultScopes })` is a front-door default policy
  used only when config omitted explicit routing choices.
- `runtime.config` is deep-readonly after construction.
- governance posture is selected by constructing a governance port, not by
  mutating config at runtime.

Removed or invalid fields fail fast during config load. Unknown fields may be
stripped only in forensic inspect paths where the loader explicitly marks the
result degraded.

## Skill Routing

`skills.routing.enabled` controls whether skill-first routing is active.
`skills.routing.scopes` is the explicit scope allowlist for routing visibility.
Scope alone does not make a skill routable; the skill must also expose a
compiled selection signal.

Interactive hosted front doors use `routingDefaultScopes=["core", "domain"]`
so skill-first routing is available without overriding explicit config.

## Verification

`verification.defaultLevel` selects the default check set. Configured command
checks remain advisory until the runtime records verification outcome through
the verification authority surface.

multi-root tasks expand default verification checks per target root.
auto-discovered `package.json` scripts run through the root package manager so
workspace-local scripts keep their expected environment.

## Security And Execution

Security config owns command policy, filesystem and network boundary policy,
credential vault settings, loop detection, and execution backend defaults.

`exec.workdir` is validated against the current task target roots before command
execution evidence can become authoritative. Box execution config controls the
default sandbox image, resource limits, guest workspace path, network posture,
and garbage-collection behavior.

## Hosted Cache

Provider token-cache policy is a hosted-session setting rather than a
top-level `BrewvaConfig` key. Hosted settings may pass normalized cache policy
through gateway, agent-engine, and provider-core.

Supported hosted cache policy fields are retention, write mode, scope, and
reason. Provider-specific cache details remain in
`@brewva/brewva-provider-core`.

## Schedule, Parallel, Channels, Infrastructure

- `schedule` controls intent projection, leases, recovery catch-up, and the
  optional self-improve recurring policy.
- `parallel` controls runtime parallelism ceilings.
- `channels` controls external orchestration limits and access control.
- `infrastructure` controls event storage and context-budget thresholds.

## Schema Maintenance

Regenerate the JSON schema after config type/default changes:

```bash
bun run generate:schema
```

Use `bun run docs:inventory` after adding, removing, or renaming top-level
config keys.
