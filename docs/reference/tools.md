# Reference: Tools

Tool registry entrypoint: `packages/brewva-tools/src/index.ts`.

This page is the stable entry for managed-tool semantics. Exact tool names are
generated below; family pages explain boundaries, authority, and failure
semantics without repeating the full inventory.

## Package Shape

`@brewva/brewva-tools` is a mechanism package with a narrow root and explicit
subpaths:

- `@brewva/brewva-tools`
- `@brewva/brewva-tools/contracts`
- `@brewva/brewva-tools/registry`
- `@brewva/brewva-tools/runtime-port`
- `@brewva/brewva-tools/navigation`
- `@brewva/brewva-tools/execution`
- `@brewva/brewva-tools/memory`
- `@brewva/brewva-tools/delegation`
- `@brewva/brewva-tools/workflow`

The root builds the default bundle. `contracts/` owns pure vocabulary,
`registry/` owns managed-tool metadata and capability scoping, `runtime-port/`
owns controlled runtime helper ports, and family subpaths expose curated
factories plus family-specific semantic helpers. Model routing policy belongs
to `@brewva/brewva-gateway/model-routing`.

Tool descriptor/catalog protocol types are imported from
`@brewva/brewva-substrate/tools`. There is no standalone
`@brewva/brewva-tool-protocol` package or compatibility import path.

## Runtime Capability Proof

Managed tools receive `ToolRuntimeAdapterPort`, not the hosted or operator port.
The registry owns a static inventory,
`BREWVA_TOOL_RUNTIME_CAPABILITY_PATHS`, containing allowed leaf paths under
`ops.*` and `extensions.tools.*`. `authority.*`, `inspect.*`, and `operator.*`
are not tool capability namespaces.

The inventory is generated by `bun run tools:capability-inventory` from the
`BrewvaToolRequiredCapability` type union. The check mode is part of
`bun run check`, so runtime surface changes must update the generated inventory
without walking a live runtime object.

`createCapabilityScopedToolRuntime(...)` validates every registry-declared
capability against that static inventory before constructing a scoped runtime.
Unknown paths fail closed and do not require walking or instantiating live
runtime services.

## Capability And Consequence Disclosure

Managed-tool `requiredCapabilities` remain enforcement, not documentation.
Capability-scoped runtime ports are constructed only from declared capability
paths; undeclared paths are unavailable to the tool implementation.

Disclosure is layered on top of that enforcement. Managed metadata and runtime
governance expose the tool action class, expected recoverability, visibility,
and recovery preparation so hosted context and operator inspection can describe
what kind of commitment a tool is expected to make. These fields do not grant
authority. The runtime still derives the final commitment posture from event
receipts, mutation records, recovery records, and conservative warnings.

Use the consequence vocabulary when explaining effect posture:

- `recoverability`: `observe_only`, `reversible`, `compensatable`,
  `manual_recovery`, or `irreversible`
- `visibility`: `local_only`, `workspace_visible`, `externally_observable`, or
  `credential_sensitive`
- `recoveryPreparation`: `none`, `workspace_patchset`, `compensation`, or
  `manual`

Exact reversibility is evidence-backed. A tool may prepare a workspace patchset,
but it is only treated as reversible after execution records an applicable undo
handle or reversible mutation receipt.

## External Capability Authority

External SaaS, CLI, MCP, credential, publish, deploy, and operator authority is
not granted by a skill card or by a provider appearing in the prompt. The
hosted gateway records a `capability_selection_recorded` receipt before
materializing the tool surface. Only selected capabilities can expose matching
operator-surface tools or pass the capability-aware quality gate.

The current implementation is intentionally deterministic. Explicit
`/capability:name` or `@capability:name` signals, policy defaults, and
deterministic filters can select a capability. Embedding ranking and LLM
fallback are reserved and inactive; no selected receipt means no external
authority.

Raw external CLI use is guarded by command classification. Account-scoped CLIs
such as `gh`, `aws`, `gcloud`, and `op`, plus publish/push/login commands such
as `npm publish`, `pnpm publish`, `cargo publish`, and `docker push`, require a
matching selected capability receipt. Ordinary package-manager commands such as
`npm test` do not become external authority just because they share the same
binary.

## Family Map

- Navigation, source inspection, browser artifacts, grep, output search, source intelligence,
  AST, LSP, and git observation: `docs/reference/tools/navigation.md`
- Execution, shell/process adapters, box routing, and managed process registry:
  `docs/reference/tools/execution.md`
- Memory, recall, precedent, solution records, knowledge, workbench note/evict
  operations, and iteration facts:
  `docs/reference/tools/memory-and-recall.md`
- Delegation, subagents, A2A, questions, and review synthesis:
  `docs/reference/tools/delegation.md`
- Workflow, worker results, task state, scheduling, follow-up,
  resource leases, tape, ledger, observability, cost, rollback, and reasoning
  continuity:
  `docs/reference/tools/workflow-and-scheduling.md`

## Generated Inventory

<!-- generated:tools-inventory start -->

> Generated by `bun run docs:inventory`. Do not edit this block by hand.

Tool count: 92.

- `agent_broadcast`
- `agent_list`
- `agent_send`
- `browser_click`
- `browser_close`
- `browser_diff_snapshot`
- `browser_fill`
- `browser_get`
- `browser_open`
- `browser_pdf`
- `browser_screenshot`
- `browser_snapshot`
- `browser_state_load`
- `browser_state_save`
- `browser_wait`
- `code_callees`
- `code_callers`
- `code_cycles`
- `code_deps`
- `code_digest`
- `code_outline`
- `code_reverse_deps`
- `code_surface`
- `cost_view`
- `discover_skills`
- `exec`
- `follow_up`
- `git_diff`
- `git_log`
- `git_status`
- `grep`
- `inbox_query`
- `iteration_fact`
- `knowledge_capture`
- `knowledge_search`
- `ledger_query`
- `look_at`
- `lsp_code_action`
- `lsp_definition`
- `lsp_diagnostics`
- `lsp_file_rename`
- `lsp_format`
- `lsp_hover`
- `lsp_implementation`
- `lsp_references`
- `lsp_rename`
- `lsp_status`
- `lsp_type_definition`
- `obs_query`
- `obs_slo_assert`
- `obs_snapshot`
- `output_search`
- `precedent_audit`
- `precedent_sweep`
- `process`
- `question`
- `reasoning_checkpoint`
- `reasoning_revert`
- `recall_curate`
- `recall_search`
- `resource_lease`
- `resource_read`
- `rollback_last_patch`
- `schedule_intent`
- `source_patch_apply`
- `source_patch_prepare`
- `source_read`
- `subagent_cancel`
- `subagent_fanout`
- `subagent_fork`
- `subagent_knowledge_adopt`
- `subagent_run`
- `subagent_run_diagnostic`
- `subagent_status`
- `tape_handoff`
- `tape_info`
- `tape_search`
- `task_add_item`
- `task_record_acceptance`
- `task_record_blocker`
- `task_resolve_blocker`
- `task_set_spec`
- `task_update_item`
- `task_view_state`
- `workbench_compact`
- `workbench_evict`
- `workbench_note`
- `workbench_undo_evict`
- `worker_results_apply`
- `worker_results_merge`
- `worker_results_reject`
- `workflow_status`
<!-- generated:tools-inventory end -->

## Runtime Contract

`buildBrewvaTools()` builds the repo-owned default managed-tool bundle. Managed
tools do not receive the raw runtime. They receive a capability-scoped facade
derived from declared `requiredCapabilities`.

Boundary rules:

- external integrations should depend on semantic runtime ports, not raw
  runtime internals
- managed-tool metadata and required capabilities are owned by
  `@brewva/brewva-tools/registry`
- family adapters must not redeclare required runtime capabilities; capability
  truth is single-sourced in the managed-tool registry
- bundled tools that need repo-owned hooks receive them explicitly
- capability declarations are fail-closed; a missing runtime capability blocks
  the tool instead of falling back to hidden authority
- external authority declarations are selected through capability manifests and
  durable selection receipts, not through `SKILL.md`
- tool outputs become replay-visible through runtime receipts, event tape, and
  verification posture
- internal process, host command, and box execution use Effect scopes,
  finalizers, streams, and timeout/schedule helpers for lifecycle mechanics;
  those mechanics do not replace capability checks or durable receipts
- host, box, and process-session state is owned by the package-created
  `ManagedExecProcessRegistryRuntime`; module-level default registries and
  exported global session maps are not stable ownership paths

## Target Roots And Read Scope

Most workspace tools are scoped by current task target roots. Command execution
and repository-local reads must honor those roots before they become
authoritative evidence.

Target roots are resolved from the task target descriptor plus existing
absolute paths explicitly mentioned in the latest turn input. Mentioned files
resolve through their containing directory; repository markers promote those
anchors to repository roots. The resulting extra roots are read scope, not
authority grants for unrelated effects. Prompt-mentioned roots are
canonicalized to real paths before filtering so symlinks cannot disguise
shallow host roots. Shallow home, volume, mount, and temp roots are ignored
instead of becoming broad read roots.

Repository-scoped retrieval filters repository artifacts to the current task
target roots. browser outputs remain workspace-root scoped because browser
sessions can observe artifacts outside a package-local path boundary.

## Transaction Boundary

Managed tools are not a distributed transaction system. Subagent, workflow, and
channel tools can coordinate work, but they do not create generalized
compensation graphs or automatic partial-failure repair.

No cross-agent saga behavior.

The stable transaction boundary remains the runtime tool-call receipt. Broader
orchestration is opt-in control-plane behavior layered over explicit receipts.

## Inspection

Use `brewva inspect`, `runtime.tape.project(...)`, and capability-scoped
`ops.*` read helpers to read recorded outcomes. Use the
generated inventory above when a code change adds, removes, or renames a tool.
Use family pages when deciding where a tool belongs or which boundary it must
respect.
