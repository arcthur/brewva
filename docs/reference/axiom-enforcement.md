# Reference: Axiom Enforcement

This page is a Tier-1 derived view of which project rules enforce each design
axiom (see `docs/architecture/design-axioms.md`, axiom 18, "Descriptive metadata
derives views, never authority", and `docs/reference/skill-navigation.md` for the
same derivation discipline).

Enforcing rules are parsed from the `(axiom N)` source tags in
`skills/project/shared/critical-rules.md` and
`skills/project/shared/anti-patterns.md`. Precedent decisions are parsed from the
`## Axioms` section of accepted records under `docs/research/decisions/`. Those
rule docs and the axiom list are the source of record; this view is a projection
that never feeds runtime selection. An axiom with no enforcing rule is shown as
negative space rather than papered over.

Regenerate with `bun run docs:axiom-enforcement`. The block below is generated;
do not edit it by hand.

<!-- generated:axiom-enforcement start -->

> Generated by `bun run docs:axiom-enforcement`. Do not edit this block by hand.

Axioms: 18. Tagged rules: 31. Axioms with no enforcing rule (negative space): 8.

### Axiom 1 — `Attention belongs to the model.`

Enforced by:

- `anti-patterns.md` — Creating a second memory store for Attention Options instead of using workbench pins, session tape evidence, recall, and repository precedents.
- `anti-patterns.md` — Materializing unbounded context through `attention_options`; options return candidate cards and `attention_consume` is the reveal boundary.
- `anti-patterns.md` — Treating `attention_ignore` as global policy rather than session-scoped advisory suppression.
- `critical-rules.md` — Keep Attention Options narrow: `attention_options` returns bounded candidate cards, `attention_consume` reveals selected content, `attention_pin` writes workbench pins, `attention_ignore` is session-scoped advisory suppression, and `attention_verify_plan` returns only a verification recipe. Do not add a second memory store or hidden context admission path.

Precedent decisions: `context-operating-system-and-compaction-physics`, `derivation-direction-invariant-and-skill-navigation`, `trust-substrate-optimization-axis`

### Axiom 2 — `Adaptive logic stays out of the kernel.`

Enforced by:

- `critical-rules.md` — Keep runtime governance deterministic and avoid embedding adaptive cognition in the kernel.

Precedent decisions: `context-operating-system-and-compaction-physics`, `derivation-direction-invariant-and-skill-navigation`, `session-index-read-model-engine`

### Axiom 3 — `Subtraction beats switches.`

Enforced by:

- `anti-patterns.md` — Re-exposing removed low-level tuning knobs as public config.
- `anti-patterns.md` — Reintroducing `@brewva/brewva-box`, `@brewva/brewva-tui`, or generic `@brewva/brewva-ingress` package identities without a new accepted ownership decision.
- `anti-patterns.md` — Reintroducing hosted context materialization plan/commit DAGs instead of direct lifecycle calls owned by hosted context modules.
- `anti-patterns.md` — Reintroducing or presenting removed `@brewva/brewva-runtime/internal` helpers as a default integration surface or stable product contract.
- `anti-patterns.md` — Reintroducing runtime-stored context evidence slots or legacy `prompt.getStability`, `prompt.getTransientReduction`, and `providerCache.getObservation` ports.
- `critical-rules.md` — Do not reintroduce `skills.packs`-style filtering or legacy `base/pack/project` taxonomy.

Precedent decisions: `derivation-direction-invariant-and-skill-navigation`, `gateway-control-tape-and-receipt-first-control-plane`, `hosted-control-plane-subtraction`, `session-index-read-model-engine`, `tree-history-and-multi-writer-substrate`, `tui-rendering-performance-and-test-harness`, `tui-split-footer-native-scrollback-renderer`

### Axiom 4 — `Govern effects, not thought paths.`

Enforced by:

- `anti-patterns.md` — Letting `@brewva/brewva-mcp-adapter` decide managed-tool capabilities or hosted action policy instead of only translating MCP protocol surfaces.

Precedent decisions: `effect-approval-and-rollback-closure`

### Axiom 5 — `Every commitment has a receipt.`

Enforced by:

- `critical-rules.md` — Keep compaction commit single-receipt and async: `session_compact` is the durable authority, while history baselines are derived from event tape and in-memory cache.

Precedent decisions: `effect-approval-and-rollback-closure`, `gateway-control-tape-and-receipt-first-control-plane`

### Axiom 6 — `Tape is commitment memory.`

Enforced by:

- `anti-patterns.md` — Treating `.brewva/session-index/session-index.sqlite` as source-of-truth memory or replay authority.
- `anti-patterns.md` — Writing history-view baseline artifact files as compaction authority instead of deriving baseline state from `session_compact` receipts.
- `critical-rules.md` — Keep continuation anchors replayable: `session.continuationAnchor` and `tape_handoff` record anchors that Work Cards, transcripts, export bundles, channel inspect, and hosted context can display without becoming new truth stores.
- `critical-rules.md` — Keep SQLite session index state rebuildable and non-authoritative; event tape remains replay authority.

Precedent decisions: `context-operating-system-and-compaction-physics`, `effect-approval-and-rollback-closure`, `gateway-control-tape-and-receipt-first-control-plane`, `hosted-control-plane-subtraction`, `tree-history-and-multi-writer-substrate`, `trust-substrate-optimization-axis`

### Axiom 7 — `Inconclusive is honest governance.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: `gateway-control-tape-and-receipt-first-control-plane`, `trust-substrate-optimization-axis`

### Axiom 8 — `Graceful degradation beats hidden cleverness.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: `durable-steering-inbox`, `effect-approval-and-rollback-closure`

### Axiom 9 — `Resource expansion is negotiated, not assumed.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: _none._

### Axiom 10 — `Recovery is model-native, not kernel choreography.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: `durable-steering-inbox`

### Axiom 11 — `Same evidence is not shared authority.`

Enforced by:

- `anti-patterns.md` — Adding duplicate inspect dashboards or one-off shell/channel/CLI renderers that bypass the shared Work Card projection payload.
- `critical-rules.md` — Preserve "same evidence, different authority": Work Cards, transcripts, channel inspect, and hosted dynamic context may orient over the same evidence, but kernel admission, capability receipts, sandbox posture, and adoption authority remain separate and authoritative.

Precedent decisions: `authority-rings-and-projection-unification`, `durable-steering-inbox`, `effect-approval-and-rollback-closure`, `gateway-control-tape-and-receipt-first-control-plane`, `hosted-control-plane-subtraction`, `session-index-read-model-engine`, `tree-history-and-multi-writer-substrate`, `trust-substrate-optimization-axis`, `tui-rendering-performance-and-test-harness`, `tui-split-footer-native-scrollback-renderer`

### Axiom 12 — `Product loops are projections, not runtime state machines.`

Enforced by:

- `critical-rules.md` — Treat `receive -> orient -> authorize -> act -> verify -> continue` as the default product-loop grammar for operator and model-facing surfaces, not as a runtime state machine.

Precedent decisions: `hosted-control-plane-subtraction`

### Axiom 13 — `Repository governance stays adjacent to the kernel.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: _none._

### Axiom 14 — `Documentation hierarchy follows authority hierarchy.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: `authority-rings-and-projection-unification`, `derivation-direction-invariant-and-skill-navigation`, `durable-steering-inbox`

### Axiom 15 — `Public width should compress toward authority width.`

Enforced by:

- `anti-patterns.md` — Exporting provider-core streaming parse projection helpers such as `partialize` or registry construction as root public APIs.
- `anti-patterns.md` — Restoring forensic inspect dump as the default operator surface instead of the schema-tagged Work Card plus explicit drill-downs.

Precedent decisions: `authority-rings-and-projection-unification`, `context-operating-system-and-compaction-physics`, `hosted-control-plane-subtraction`

### Axiom 16 — `Kernel contracts admit only correctness-bearing judgments.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: _none._

### Axiom 17 — `Platform growth stays opt-in until multi-agent semantics mature.`

Enforced by: _no tagged rule — negative space._

Precedent decisions: `gateway-control-tape-and-receipt-first-control-plane`

### Axiom 18 — `Descriptive metadata derives views, never authority.`

Enforced by:

- `anti-patterns.md` — Adding a `Run skill` path or allowing SkillCards to grant tools, accounts, budgets, model routes, or workflow execution.
- `anti-patterns.md` — Authoring a second schema source for provider tool arguments instead of deriving advisory streaming parse from the canonical TypeBox schema.
- `anti-patterns.md` — Editing generated distribution artifacts by hand.
- `anti-patterns.md` — Letting a verifier adapter directly hard-gate admission; hard gates require a verification gate manifest converted into kernel policy input.
- `anti-patterns.md` — Letting an advisory extension output widen capability, kernel, sandbox, source, or adoption authority.
- `anti-patterns.md` — Recreating hidden local-hook policy with `block_tool` or any equivalent blocking result outside explicit kernel admission and verification gates.
- `critical-rules.md` — Advisory extension manifests fail closed. `context.contributor` manifests must declare `pure`, `read_tape`, or `read_fs`; local hooks stay advisory and must not recreate hidden `block_tool` policy.
- `critical-rules.md` — Keep SkillCards advisory catalog cards with authority posture `none`. A SkillCard, `$skill` mention, or `/skills` catalog entry must not grant tools, accounts, budgets, model routes, or a `Run skill` execution path.
- `critical-rules.md` — Verifier adapters are advisory by default. Kernel defer or abort behavior requires an explicit verification gate manifest converted into kernel policy input; adapters must not mutate admission or approval state directly.

Precedent decisions: `derivation-direction-invariant-and-skill-navigation`

<!-- generated:axiom-enforcement end -->
